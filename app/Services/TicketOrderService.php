<?php
namespace App\Services;

// use Illuminate\Support\Facades\Auth;

class TicketOrderService
{

    public function get_ticket_orders($user_id) {
        // $cart = Cart::where('status', 1)->where('user_id', $user_id)->first();
        // return $cart->id;
    }

    
}
